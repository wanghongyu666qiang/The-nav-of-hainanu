# 2026-05-30

## 编码转换：UTF-8-SIG → GBK

- 将项目 11 个 .cpp/.h/.txt 源码文件从 UTF-8-SIG（带BOM）转换为 GBK 编码
- 去除了 BOM 头，清除了 175 个无法被 GBK 映射的 PUA 私用区字符（来自 VS Code 字体插件的装饰字符）
- 目的：在 Windows 命令行（GBK 终端）下用 MSVC/MinGW 编译不报编码错误
- 2 个 .md 文件跳过（字节已损坏，多种编码均无法解码，但不影响编译）
- 原文件已备份到 `D:\The_work_of_datastruct\The_navigation_of_hainanu_utf8_backup\`
- **注意**：文件中的中文注释/字符串本身存在历史乱码（非本次转换引入），GBK 终端下 cout 中文输出可能仍显示乱码

## 编码转换（第二次，git restore 后重做）

- 先用 `git restore` 从仓库恢复所有源码文件（UTF-8，无 PUA 字符，干净）
- 将 10 个 UTF-8 文件直接编码为 GBK（printscene.cpp 本就是 GBK，跳过）
- 在 `.vscode/settings.json` 中添加 `"files.encoding": "gbk"` 让 VS Code 按 GBK 显示
- VS Code 需 Reload Window 后生效
