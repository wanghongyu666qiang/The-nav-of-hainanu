# 2026-06-02

## 重构：伪邻接表 → 显式邻接矩阵

- 删除了 `Edge` 结构体，Scene 回归纯数据（id/name/description）
- Graph 新增 `adj`（权重矩阵）和 `roadType`（道路类型矩阵），以 `vector<vector<int>>` 存储
- 涉及文件：Graph.h、Graph.cpp、file_io.cpp、addScene.cpp、deleteScene.cpp、addRoad.cpp、deleteRoad.cpp、main.cpp、render.cpp 共 9 个
- 编译零警告，Dijkstra/DFS 均验证通过

## 回滚：多景点顺序游览

- 已通过 git checkout 移除全部多景点功能，代码恢复到仅含邻接矩阵重构的状态
