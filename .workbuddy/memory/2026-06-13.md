## 2026-06-13

### Web 部署方案完成
- 采用 `server.py` (Python 标准库, ~28行) 作为 HTTP 中间层
  - 无需 pip install, 无需改 main.cpp
  - 原理: 收浏览器请求 → subprocess.run 启动 main.exe, stdin 喂菜单参数 → 抓 stdout 返回
- 修复 bug: `capture_output=True` 和 `stderr=subprocess.DEVNULL` 冲突, 改为 `stdout=subprocess.PIPE`
- 本机测试通过: `http://localhost:8080/api?a=4` 返回景点列表, `api?a=1&d=1 8 0` 返回最短路径

### 前端 UI
- 修改 `ui/map_haidian.html`: flex 布局, 地图左 + 查询面板右
- 面板: 起点/终点下拉框(JS 从 /api?a=4 动态填充), 步行/车行单选, 查询按钮, <pre> 结果区
- 零依赖 (纯 HTML+CSS+原生 fetch)

### 回退到纯 C++ 命令行版 (22:10)
- 用户反馈 web 服务层（server.py + subprocess + fetch）增加了不必要的复杂度
- `git reset --hard 2953818` 回退到 web 服务引入之前的最新 commit
- 手动删除 `server.py`（git reset 不删未跟踪文件）
- 重新编译 `main.exe`，验证运行正常
- 决定：先专注 C++ 算法本身，web 界面作为课程报告"可选扩展"提一句即可

### 下一步
- 专注 C++ 命令行版本的算法完善和课程报告撰写
