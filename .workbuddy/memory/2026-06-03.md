# 2026-06-03

## B/S 架构：C++ HTTP 服务端 + Python 调度层

- 创建 `algo2.cpp`：C++ 命令行工具，输出纯数值（距离+索引列表），供 Python 调用
- 创建 `server.py`：Python HTTP 服务端（标准库），接收前端 fetch 请求，调用 algo2.exe 计算，JSON 响应
- 创建 `index.html`：前端地图，通过 fetch('/api/shortest') 和 fetch('/api/allpaths') 调用后端
- 创建 `start_server.bat`：一键启动服务端
- 架构：浏览器 → HTTP → Python → subprocess → C++ algo2.exe → 返回 JSON
- 编译命令相同（g++ -std=c++11 -O0），仅需 `python server.py` 启动服务
- 中文编码已解决：Python json.dumps(ensure_ascii=False) 处理中文
